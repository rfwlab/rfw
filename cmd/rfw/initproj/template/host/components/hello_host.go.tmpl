package components

import "github.com/rfwlab/rfw/v2/host"

func RegisterHelloHost() {
	host.Register(host.NewHostComponent("HelloHost", func(_ map[string]any) any {
		return map[string]any{"greeting": "hello from host"}
	}))
}
